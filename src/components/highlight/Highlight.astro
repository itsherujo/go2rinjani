---
import ImageBanner from "../ui/ImageBanner.astro";
import { getLangFromUrl, useTranslations, getLocalizedPath } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang, "home");
---

<ImageBanner
  imageSrc="https://images.unsplash.com/photo-1576867757603-05b134ebc379?q=80&w=2000&auto=format&fit=crop"
  imageAlt="Hummus and pita"
  bgColor="bg-brand-coral"
  title={t("experience.title")}
  desc={t("experience.desc")}
  ctaLabel={t("experience.cta_label")}
  ctaHref={getLocalizedPath("/about", lang)}
/>
