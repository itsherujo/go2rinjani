---
export const prerender = true;
import HowItWorksPage from '../../../features/how-it-works/Page.astro';

export function getStaticPaths() {
  const langs = ["id", "zh", "de", "fr", "es"];
  return langs.map(lang => ({ params: { lang } }));
}
---

<HowItWorksPage />
