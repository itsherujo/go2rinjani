---
export const prerender = true;
import EventsPage from '../../features/events/Page.astro';

export function getStaticPaths() {
  const langs = ["id", "zh", "de", "fr", "es"];
  return langs.map(lang => ({ params: { lang } }));
}
---

<EventsPage />
