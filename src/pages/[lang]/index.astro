---
export const prerender = true;
import HomePage from '../../features/home/Page.astro';

export function getStaticPaths() {
  const langs = ["id", "zh", "de", "fr", "es"];
  return langs.map(lang => ({ params: { lang } }));
}
---

<HomePage />
