---
export const prerender = true;
import ContactPage from '../../../features/contact/Page.astro';

export function getStaticPaths() {
  const langs = ["id", "zh", "de", "fr", "es"];
  return langs.map(lang => ({ params: { lang } }));
}
---

<ContactPage />
