---
export const prerender = true;
import AboutPage from '../../features/about/Page.astro';

export function getStaticPaths() {
  const langs = ["id", "zh", "de", "fr", "es"];
  return langs.map(lang => ({ params: { lang } }));
}
---

<AboutPage />
