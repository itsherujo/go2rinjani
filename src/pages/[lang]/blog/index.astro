---
export const prerender = true;
import BlogPage from '../../../features/blog/Page.astro';

export function getStaticPaths() {
  const langs = ["id", "zh", "de", "fr", "es"];
  return langs.map(lang => ({ params: { lang } }));
}
---

<BlogPage />
